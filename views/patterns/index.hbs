<div class="row">
  {{#each patterns}}
    <div class="col-4">
      <figure>
        <img src="{{this.coverImage}}" class="img-fluid">
        <h2><a href="/patterns/{{this.name}}">{{this.name}}</a></h2>
      </figure>
    </div>
  {{/each}}
</div>